{# {{ dump(field.value) }} #}
<div class="form-widget">
    {% for image in field.value %}
        <a href="#" class="ea-lightbox-thumbnail" data-ea-lightbox-content-selector="#ea-lightbox-{{ image.id }}">
            <img src="/assets/img/medias/{{ image.image }}" class="img-fluid" style="max-width: 225px; width: 100%;">
        </a>
        <div id="ea-lightbox-{{ image.id }}" class="ea-lightbox">
            <img src="/assets/img/medias/{{ image.image }}" class="img-fluid">
        </div>
    {% endfor %}
</div>